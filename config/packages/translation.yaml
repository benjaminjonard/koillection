framework:
    default_locale: '%default_locale%'
    enabled_locales: ['en', 'fr', 'es', 'de', 'it']
    translator:
        paths:
            - '%kernel.project_dir%/translations/'
        fallbacks:
            - '%default_locale%'
