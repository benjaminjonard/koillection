{% extends 'layout-autorized.html.twig' %}

{% set breadcrumb = buildBreadcrumb([
    {'route': 'app_admin_index', 'trans': 'menu.admin'},
    {'route': 'app_admin_users', 'trans': 'menu.users'}
]) %}

{% block title %}
    {{ renderTitle(breadcrumb)  }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb)  }}
{% endblock %}

{% block content %}
    <div class="content-wrapper">
        <div class="title-block">
            <h1>{{ 'menu.admin'|trans }}</h1>
        </div>

        <div class="content-block">
            {{ include('/App/Admin/partials/tabs.html.twig', {current: 'users'}) }}
            <div class="block-element">
                <h5 class="header">{{ 'title.users'|trans }}</h5>
                <table class="centered striped">
                    <thead>
                    <tr>
                        <th>{{ 'label.username'|trans }}</th>
                        <th>{{ 'label.role'|trans }}</th>
                        <th>{{ 'label.disk_space_used'|trans }}</th>
                        <th>{{ 'label.disk_space_allowed'|trans }}</th>
                        <th>{{ 'label.last_connection'|trans }}</th>
                        <th>{{ 'label.actions'|trans }}</th>
                    </tr>
                    </thead>

                    <tbody>
                    {% for user in users %}
                        <tr>
                            <td>{{ user.username }}</td>
                            <td>{{ getRoleLabel(user.roles[0])|trans }}</td>
                            <td>{{ user.diskSpaceUsed|bytes }}</td>
                            <td>{{ user.diskSpaceAllowed|bytes }}</td>
                            <td>{{ user.connections|length > 0 ? user.connections|last.date|timeAgo : '-' }}</td>
                            <td>
                                <a class="waves-effect waves-light btn" href="{{ path('app_admin_user', {id : user.id}) }}">
                                    <i class="fa fa-search fa-fw"></i>
                                    {{ 'btn.details'|trans }}
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
