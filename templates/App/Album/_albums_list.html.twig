<table class="highlight items-table">
    <thead>
        <tr>
            <th></th>
            <th>{{ 'label.name'|trans }}</th>
            {% if context == 'default' %}
                <th class="center">{{ 'label.visibility'|trans }}</th>
                <th class="center">{{ 'label.actions'|trans }}</th>
            {% endif %}
        </tr>
    </thead>

    <tbody>
        {% for album in albums|naturalSorting %}
            {% set link = path('app_album_show'|applyContext, {'id': album.id}) %}
            <tr class="list-element resize-element" data-title="{{ album.title }}" data-filter-target="element">
                <td>
                    <a class="table-link" href="{{ link }}"></a>
                    {% if album.image %}
                        <div class="image-wrapper placeholder placeholder--circle" data-controller="lazy-loading">
                            <img data-src="{{ asset(album.image) }}"
                                 data-lazy-loading-target="image"
                            >
                        </div>
                    {% endif %}
                </td>
                <td>
                    <a class="table-link" href="{{ link }}"></a>
                    {{ album.title|u.truncate(50, '...') }}
                </td>
                {% if context == 'default' %}
                    <td class="center">
                        {% include 'App/_partials/_visibility.html.twig' with {'visibility': album.finalVisibility} only %}
                    </td>

                    <td class="table-actions center">
                        <a href="{{ path('app_album_edit', {id: album.id}) }}" title="{{ 'tooltip.edit'|trans }}">
                            <i class="fa fa-pencil fa-fw"></i>
                        </a>
                        {% include 'App/_partials/_icon/_delete.html.twig' with {
                            path: path('app_album_delete', {id: album.id}),
                            message: 'message.delete.album'
                        } only %}
                    </td>
                {% endif %}
            </tr>
        {% endfor %}
    </tbody>
</table>