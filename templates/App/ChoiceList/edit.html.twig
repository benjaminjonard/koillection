{% extends 'layout.html.twig' %}

{% set breadcrumb = buildBreadcrumb([{'route': 'app_choice_list_index', 'trans': 'menu.choice_lists'}], null, 'breadcrumb.edit_choice_list') %}

{% block title %}
    {{ renderTitle(breadcrumb) }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb) }}
{% endblock %}

{% block content %}
    <div class="title-block">
        <h1>{{ 'title.edit_choice_list'|trans({'%list%': choiceList.name}) }}</h1>
    </div>

    <div class="content-block">
        {{ include('App/ChoiceList/_form.html.twig') }}
    </div>
{% endblock %}
