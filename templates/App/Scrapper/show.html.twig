{% extends 'layout.html.twig' %}

{% set breadcrumb = buildBreadcrumb([{'route': 'app_scrapper_index', 'trans': 'menu.scrappers'}], scrapper) %}

{% block title %}
    {{ renderTitle(breadcrumb) }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb) }}
{% endblock %}

{% block content %}
    <div class="title-block">
        <h1>{{ scrapper.name }}</h1>
    </div>

    <div></div>

    <div class="btn-holder">
        <a href="{{ path('app_scrapper_edit', {'id': scrapper.id}) }}" class="button"
           title="{{ 'tooltip.edit'|trans }}">
            <i class="fa fa-pencil fa-fw"></i>
        </a>
        {{ include('App/_partials/_icon/_delete.html.twig', {
            path: path('app_scrapper_delete', {id: scrapper.id}),
            message: 'message.delete.scrapper'
        }) }}
    </div>

    <div class="content-block">
        <div class="block-element">
            <h2 class="header">{{ 'title.fields'|trans }}</h2>

            <table class="striped">
                <thead>
                <tr>
                    <th>{{ 'label.name'|trans }}</th>
                    <th>{{ 'label.path'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                <tr class="list-element">
                    <td>{{ 'label.name_path'|trans }}</td>
                    <td>{{ scrapper.namePath }}</td>
                </tr>
                <tr class="list-element">
                    <td>{{ 'label.image_path'|trans }}</td>
                    <td>{{ scrapper.imagePath }}</td>
                </tr>
                {% for dataPath in scrapper.dataPaths %}
                    <tr class="list-element">
                        <td>{{ dataPath.name }}</td>
                        <td>{{ dataPath.path }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

