{% extends 'layout.html.twig' %}

{% set breadcrumb = buildBreadcrumb([{'route': 'app_tag_category_index', 'trans': 'menu.tag_categories'}], null, 'breadcrumb.add_tag_category') %}

{% block title %}
    {{ renderTitle(breadcrumb) }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb) }}
{% endblock %}

{% block content %}
    <div class="title-block">
        <h1>{{ 'title.add_tag_category'|trans }}</h1>
    </div>

    <div class="content-block">
        {{ include('App/TagCategory/_form.html.twig') }}
    </div>
{% endblock %}
