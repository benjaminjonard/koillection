{% extends 'layout.html.twig' %}

{% set breadcrumb = buildBreadcrumb([{'route': 'app_tools_index', 'trans': 'menu.tools'}]) %}

{% block title %}
    {{ renderTitle(breadcrumb)  }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb)  }}
{% endblock %}

{% block content %}
    <div class="content-wrapper">
        <div class="title-block">
            <h1>{{ 'menu.tools'|trans() }}</h1>
        </div>

        <div></div>

        <div class="btn-holder">
        </div>

        <div class="content-block">
            <div class="block-element">
                <h5 class="header">{{ 'title.exports'|trans }}</h5>
                <a href="{{ path('app_tools_export') }}" target="_blank" class="btn waves-effect waves-light">
                    <i class="fa fa-print fa-fw"></i>{{ 'btn.export'|trans }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}
