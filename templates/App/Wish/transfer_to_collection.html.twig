{% extends 'layout.html.twig' %}

{% set breadcrumb = buildBreadcrumb([{'route': 'app_wishlist_index', 'trans': 'menu.wishlists'}], wish.wishlist, 'breadcrumb.transfer_wish') %}

{% block title %}
    {{ renderTitle(breadcrumb)  }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb)  }}
{% endblock %}

{% block content %}
    <div class="title-block">
        <h1>{{ 'title.transfer_wish'|trans }}</h1>
    </div>

    <div class="content-block">
        {{ form_start(form) }}
        {% do form.data.setRendered %}
        <div class="block-element">
            <div class="row">
                <h5 class="header">{{ 'title.infos'|trans }}</h5>
                <div class="col s12 m5 offset-m1">
                    {{ form_row(form.name, {'label' : 'label.name'}) }}
                </div>
                <div class="col s12 m5 offset-m0">
                    {{ form_row(form.image.uploadedFile, {'attr': {'src' : item.image ? item.image.path : null}}) }}
                </div>
            </div>
            <div class="row">
                <div class="col s12 m4 offset-m1">
                    {{ form_row(form.collection, {'label' : 'label.collection'}) }}
                </div>
                <div class="col s12 m3 offset-m0">
                    {{ form_row(form.quantity, {'label' : 'label.quantity'} ) }}
                </div>
                <div class="col s12 m3 offset-m0">
                    {{ form_row(form.visibility, {'label' : 'label.visibility'} ) }}
                </div>
            </div>
            <div class="row">
                <div class="col s12">
                    {{ form_label(form.tags, 'label.tags') }}
                    <div class="autocomplete" id="multiple">
                        <div class="ac-input">
                            <input type="text" id="tagsAutocomplete" data-target="tagsDropdown" data-beloworigin="true" autocomplete="off">
                        </div>
                        <div class="ac-tags">
                        </div>
                        <ul id="tagsDropdown" class="dropdown-content ac-dropdown"></ul>
                    </div>
                    {{ form_widget(form.tags, {'label' : 'label.tags', 'attr': {'class' : 'hidden tags-holder'}}) }}
                </div>
            </div>
        </div>

        <div class="row block-element">
            <h5 class="header">{{ 'title.additional_fields'|trans }}</h5>
            <div class="col s12 m4 offset-m1 input-field">
                <select class="selectFieldType" {% if fieldsType|length <= 10 %} data-minimum-results-for-search="-1"{% endif %}>
                    <option value=""></option>
                    {% for key, fieldType in fieldsType %}
                        <option value="{{ key }}">{{ fieldType|trans }}</option>
                    {% endfor %}
                </select>
                <label>{{ 'label.new_data'|trans }}</label>
            </div>
            <div class="col s12 m4 offset-m0">
                {{ form_row(form.template, {'label' : 'label.apply_template', 'attr' : {'class' : 'selectTemplate'}}) }}
            </div>
            <div class="col s10 m2">
                <button class="btn align-with-inputs btn-common-fields" data-collection-id="">{{ 'btn.apply_collection_data'|trans }}</button>
            </div>
            <div class="clearfix"></div>
            {{ include('App/Item/_data.html.twig', {data: item.data}) }}
        </div>

        <div class="form-btn-holder">
            <button type="submit" class="btn waves-effect waves-light">{{ 'btn.submit'|trans }}</button>
            <a href="{{ path('app_wishlist_show', { id : wish.wishlist.id }) }}" class="waves-effect waves-light btn red darken-1">{{ 'btn.cancel'|trans }}</a>
        </div>
        <div class="clearfix"></div>
        {{ form_end(form) }}
    </div>
{% endblock %}
