<ol class="breadcrumb">
    {% for element in breadcrumb %}
        <li class="breadcrumb-item {{ loop.last ? 'active' }}">
            {{ include('App/_partials/breadcrumb-'~ element.type ~'.html.twig', {'element' : element}) }}
        </li>
    {% endfor %}
</ol>
