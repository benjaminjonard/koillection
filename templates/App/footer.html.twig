<footer>
    {{ 'global.created_at'|trans({'%date%': object.createdAt|date(app.user.dateFormat, app.user.timezone)}) }}
    {% if object.updatedAt %}
        · {{ 'global.updated_at'|trans({'%date%': object.updatedAt|date(app.user.dateFormat, app.user.timezone) }) }}
    {% endif %}

    · {{ ('global.seen_counter.' ~ class)|trans({'%count%': object.seenCounter + 1}) }}
</footer>
