{% extends 'layout-default.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link type="text/css" rel="stylesheet" href="{{ preload(asset('build/css/translation.css')) }}" nonce="{{ csp_nonce() }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset("bundles/translation/js/webui.js") }}" nonce="{{ csp_nonce() }}"></script>
{% endblock %}

